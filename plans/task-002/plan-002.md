APPROVED: The plan correctly delivers Feature 1's data-tables half with a clear MVP table set justified inline against `docs/v105-item-format.md`, embeds files via `include_str!` from the verified `assets/excel/v105/` directory, uses only `std::sync::OnceLock` (no new deps in `Cargo.toml`), specifies a tab-separated parser with explicit `Expansion` row handling and CRLF stripping, defines `pub(crate)` typed lookup APIs with anchor unit tests, and enumerates risks (column-name drift, code collisions, dead_code, code-padding from Huffman) with concrete mitigations and a sound Definition of Done. A developer can implement directly from it.
